<UserControl x:Class="AAD.ImmoWin.WpfApp.Views.VastgoedModuleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AAD.ImmoWin.WpfApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             IsEnabled="{Binding IsEnabled}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Border BorderBrush="Black" BorderThickness="1" Margin="5" Grid.Column="0">
            <ContentControl Content="{Binding HuidigeVastgoedLijstViewModel}"></ContentControl>
        </Border>
        <Border BorderBrush="Black" BorderThickness="1" Margin="5" Grid.Column="1">
            <ContentControl Content="{Binding HuidigVastgoedDetailViewModel}"></ContentControl>
        </Border>
    </Grid>
</UserControl>
